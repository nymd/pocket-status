{
  "name": "frisk",
  "version": "1.0.0",
  "description": "Generate Pocket Status",
  "main": "index.js",
  "scripts": {
    "status": "node dist/index.js",
    "version": "node dist/version.js",
    "sweep": "rm -rf dist/ && tsc && node dist/sweep.js",
    "unjail": "rm -rf dist/ && tsc && node dist/unjail.js",
    "unstake_2f3da6bc2add56c": "rm -rf dist/ && tsc && node dist/unstake.js",
    "start": "rm -rf dist/ && tsc && node dist/index.js",
    "build": "rm -rf dist/ && tsc"
  },
  "repository": "https://github.com/nymd/frisk",
  "license": "MIT",
  "dependencies": {
    "@types/request-promise": "^4.1.47",
    "child_process": "^1.0.2",
    "csv-parser": "^3.0.0",
    "request": "^2.88.2",
    "request-promise": "^4.2.6",
    "typescript": "^4.2.2",
    "url": "^0.11.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.31"
  }
}
